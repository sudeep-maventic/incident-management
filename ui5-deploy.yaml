specVersion: "4.0"

metadata:
  name: firstapp.incidentmanagement

type: application

builder:
  customTasks:
    - name: ui5-task-zipper
      afterTask: generateCachebusterInfo

      configuration:
        archiveName: firstapp-incidentmanagement